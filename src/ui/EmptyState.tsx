export function EmptyState() {
  return (
    <div class="empty-state">
      <div class="empty-state-icon">✓</div>
      <div class="empty-state-title">No changes — infrastructure is up to date</div>
    </div>
  );
}
